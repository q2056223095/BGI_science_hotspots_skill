# Changelog

## 0.5.1

### 图片生成执行协议修复

本版本不改变 0.5.0 的活人感文案主架构，重点修复实际出图阶段的执行歧义。

#### 新增强制状态机

```text
未出图
→ 只生成 P1 封面
→ 等待用户确认
→ 只生成 P2
→ 等待用户继续
→ 只生成 P3
→ 等待用户继续
→ 只生成 P4
→ 等待用户继续
→ 只生成 P5
→ 完成
```

- 第一次开始做图时只能生成 P1
- P1 必须是独立 3:4 竖版
- P1 生成后必须停止并等待用户确认
- 封面确认后，P2–P5 按顺序一张一张生成
- 即使用户说“继续图4-图5”，默认仍先只生成 P4

### 明确规划与生成是两件事

- 可以一次规划 5 张轮播图
- 可以一次输出 P1–P5 的图片提示词
- 实际调用图片工具时默认一次只生成一张
- “做完整一组”“继续生成图2-图5”不得解释为多页合成

### 禁止多页合成

新增硬性禁止：

- 2×2 四宫格
- 上下双页
- 左右双页
- P4 + P5 合成
- contact sheet
- 长图串联多页
- 五页缩略图总览
- 一个画布出现多个页码

规则统一为：

```text
一次调用 = 一个画布 = 一页 = 一个页码 = 一个核心问题
```

### 后续页面视觉锁定

P1 一旦确认，P2–P5 必须继承：

- 主色和背景体系
- 字体气质
- 标题位置与层级
- 页码 / 角标样式
- 信息卡片样式
- 图标语言
- 重点色
- 科技感强度
- 留白比例

### 新增单页防拼图提示词

每次实际生成图片时，提示词开头强制写：

```text
只生成一张独立的 3:4 竖版小红书轮播图。
当前只生成 P{页码}。
一个画布只允许这一页，不得出现其他页码或其他页面内容。
禁止拼图、四宫格、双页、上下分屏、左右分屏、长图、多页总览和 contact sheet。
```

### 增加失败判定

以下结果不得当作成功交付：

- 多页合成
- 比例不是 3:4
- 出现多个页码
- 当前页混入下一页内容
- 与已确认封面视觉明显不一致

出现这些情况时，必须重新生成当前页，不得继续下一页。

### 同步更新文件

- `SKILL.md`
- `prompts/master_prompt.md`
- `templates/image_generation_template.md`
- `templates/workflow_checklist.md`
- `docs/visual_style_guide.md`
- `README.md`
- `VERSION`

---

## 0.5.0

### 新增科学科普活人感编辑层

- 新增 `docs/human_editorial_layer.md`
- 活人感从口头语和固定句式控制，转向材料、说话位置、段落推进和中文动作
- 短篇文案动笔前至少准备 3 条独立材料
- 超过 1000 字的深度稿至少准备 5 条独立材料
- 材料不足时先研究、缩题或缩短，禁止重复解释凑字数

### 重构文案工作流

旧流程：

```text
初稿
→ 去 AI 味
→ 打分
```

新流程：

```text
原始来源核验
→ 材料卡
→ 事实身份与边界
→ 唯一主要任务
→ 事实初稿
→ 活人感编辑
→ 科学边界复核
→ 文风检查
```

### 取消固定六段式

- 不再要求所有稿件机械使用“抓人开头、事实、机制、意义、边界、收束”
- 提供事件推进、证据推进、核验推进和技术推进四条可选主路径
- 每段必须增加新事实、新解释、新限制或新后果

### 强化真实感边界

- 禁止虚构读者反应、普通人故事、亲历、采访、原话和无来源精确场景
- 作者声音来自信息来路、材料取舍和有限判断
- 科学边界放在影响判断的位置，不在文末统一免责

### 重构模型化修辞检查

- 不再以“不是……而是……”允许出现几次作为核心标准
- 检查翻案腔的写作动作及其变体
- 增加洞察路标、名词化、连词密度、同构排比和假深刻检查
- 新增 `scripts/check_copy_style.py`

### 模板与示例

- 重写 `templates/xiaohongshu_copy_template.md`
- 重写 `templates/anti_ai_self_check.md`
- 重写 `templates/workflow_checklist.md`
- 新增前后对照示例
- 更新鲸落、肌肉流失和 JUNO 示例

### 第三方说明

- 新增 `THIRD_PARTY_NOTICES.md`
- 说明对 MIT 开源项目 `KKKKhazix/human-writing` 方法的参考与适配

---

## 0.4.0

### 核心定位调整

本版本将 Skill 从教育或品牌转化型科学热点内容工具，调整为科学热点小红书科普转译工具。

核心原则：

```text
先做科普，不做教育转化。
```

内容方向更新为发现型、机制型、技术型和争议反常识型，并强化事实核验、视觉参考和合规边界。

---

## 0.3.0

- 新增去 AI 味人工编辑层
- 新增 AI 味自查自纠评分模块
- 新增视觉风格统一规则
- 新增发布前质检流程

## 0.2.0

- 新增小红书文案模板
- 新增图片生成模板
- 新增工作流检查清单
- 新增示例案例

## 0.1.0

- 建立科学热点转译基础工作流
- 支持热点事实整理、文案生成和轮播图规划
